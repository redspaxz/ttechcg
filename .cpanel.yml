---
deployment:
  tasks:
    - export DEPLOYPATH=/home/ttecwymc/public_html
    - /bin/rm -f "$DEPLOYPATH/index.html"
    - /bin/rsync -av --exclude='.git/' --exclude='.env' --exclude='docker-compose.yml' /home/ttecwymc/repositories/ttechcg/ "$DEPLOYPATH/"
    - /bin/test -s "$DEPLOYPATH/index.php"

